# Customer Success Digital FTE — Stage 2 Dependencies
# Hackathon 5 | Author: Mehreen Asghar

# ── Web Framework ──────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0

# ── Data Validation ────────────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1

# ── Database (Stage 2) ─────────────────────────────────────────────────────
sqlalchemy>=2.0.48           # ORM with PostgreSQL-ready declarative syntax (2.0.48+ required for Python 3.14)
# psycopg2-binary==2.9.9    # Uncomment for PostgreSQL production deployment

# ── Testing ────────────────────────────────────────────────────────────────
pytest==8.2.0
httpx==0.27.0                # Async HTTP client used by FastAPI TestClient
pytest-asyncio==0.23.6

# ── Utilities ──────────────────────────────────────────────────────────────
python-dotenv==1.0.1         # .env file loading for DATABASE_URL etc.

# ── Stage 3 (not yet active — add when integrating live channels) ──────────
# anthropic>=0.27.0          # Claude API for LLM response generation
# twilio>=9.0.0              # WhatsApp Business API
# google-auth>=2.0.0         # Gmail API authentication
# google-api-python-client   # Gmail API client
